# Local Netlify folder
.netlify
node_modules
.env
.env.local
.env.*.local

# Vite build output — built by Netlify at deploy time, must not be committed
# (it contains baked-in env vars that trigger secret scanning)
planocorrida/

# Python artifacts (legacy, kept for safety)
__pycache__/
*.pyc
*.pyo
*.pyd
*.so
*.dist-info/
