backend:
  name: git-gateway
  branch: main

media_folder: "assets/img/uploads"
public_folder: "/assets/img/uploads"

publish_mode: editorial_workflow

# Legacy Decap configuration.
# Blog source of truth is Supabase via admin/index.html.
# Keep this file only to avoid accidental Decap bootstrap errors.
collections: []
